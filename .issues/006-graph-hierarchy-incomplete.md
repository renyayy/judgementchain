# 階層構造図（ツリー図）が未実装

## 問題

設計では「最上階の論理的なまとまりごとの階層構造図」を出力する仕様だが、
現状は **ネットワークグラフ（dagre レイアウト）のみ** で、ツリー図のエクスポートや表示がない。

## 影響

- 設計仕様の一部が未達成
- 階層の深さ（フロア）を選択して表示を切り替える機能がない

## 参考

- 設計: 「階数を選択するとその階数（=抽象度）のグラフ図が表示」
- 設計: 「論理的なまとまりの種類を選択するとその分野の階層構造図が表示」
